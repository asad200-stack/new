############################################################
# Multi-Tenant SaaS Store Platform
############################################################

# PostgreSQL connection string (Railway provides this automatically when you add Postgres)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?schema=public"

# JWT secret for signing sessions (min 32 chars)
JWT_SECRET="CHANGE_ME_TO_A_LONG_RANDOM_SECRET_32+_CHARS"

# Public app URL (optional; used for some redirects/links)
# Example on Railway: https://your-app.up.railway.app
APP_URL="http://localhost:3000"

# Storage path for uploaded media (recommended in production to be a persistent volume)
# Example on Railway Volume: /data
STORAGE_PATH="./storage"

# Optional: seed demo data automatically on first boot if DB is empty
# Set to "1" on a fresh deploy, then remove/disable.
AUTO_SEED="0"
